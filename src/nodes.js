const query = (id) => document.querySelector(id);
const trendingPreview = query("#trending-preview");
const heroContainer = query("#hero-container");
const categoriesContainer = query("#categories-buttons-container");
